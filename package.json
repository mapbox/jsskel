{
  "main": "index.js",
  "scripts": {
    "test": "retire -p && eslint *.js test && tap --cov test/*.js"
  },
  "devDependencies": {
    "eslint": "^1.6.0",
    "eslint-config-mourner": "^1.0.1",
    "retire": "*",
    "tap": "^2.1.1"
  }
}
